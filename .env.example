# ===== 本机技能目录配置 =====
# assets/image-prompts.md 的调用命令通过 $env:AGENTS_SKILLS_DIR 定位 image2-api skill。
# 推荐直接设置用户级环境变量（持久生效）：
#   [Environment]::SetEnvironmentVariable('AGENTS_SKILLS_DIR','C:\path\to\.agents\skills','User')
# 复制本文件为 .env 亦可，但 PowerShell 不会自动读取 .env，需自行加载。
AGENTS_SKILLS_DIR=
